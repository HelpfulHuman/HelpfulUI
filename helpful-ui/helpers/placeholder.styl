/**
 * Used to define browser-friendly placeholder color.
 *
 * @param  {Color} $color
 */
placeholder($color)
  &::-webkit-input-placeholder
    color $color
  &:-moz-placeholder
    color $color
  &::-moz-placeholder
    color $color
  &:-ms-input-placeholder
    color $color
