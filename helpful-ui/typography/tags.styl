@require 'options'
@require '../helpers/rules'

body
  rules($typography.body)

::selection
  rules($typography.selection)

a
  rules($typography.links)

p
  rules($typography.paragraph)

ol, ul
  rules($typography.lists.default)

ol
  rules($typography.lists.ol)

ul
  rules($typography.lists.ul)

h1, .h1,
h2, .h2,
h3, .h3,
h4, .h4,
h5, .h5,
h6, .h6
  rules($typography.headings.default)

for i in 1..6
  h{i},.h{i}
    rules($typography.headings['h' + i])

hr
  rules($typography.hr)

pre, code, kbd, samp
  rules($typography.code)

pre
  rules($typography.pre)

small
  font-size 85%

b, strong
  font-weight bold

i, em
  font-style italic

sub, sup
  position relative
  font-size 75%
  line-height 0

sup
  top -0.5em

sub
  bottom -0.5em

del
  text-decoration line-through

abbr[title], dfn[title]
  rules($typography.definitions)

q, blockquote
  quotes none

  &:before,
  &:after
    content ''
    content none
